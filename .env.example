# PhishGuard — example environment file (commit-safe)
# Copy to `.env` and fill in real values.

# Database (Spring Boot)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=phishguard
DB_USER=postgres
DB_PASSWORD=postgres

# AI service base URL (used by backend when integrated)
AI_SERVICE_URL=http://localhost:8001

