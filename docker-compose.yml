# PhishGuard — chỉnh image, port và biến môi trường theo backend / ai-service thực tế
services:
  # backend:
  #   build: ./backend
  #   ports:
  #     - "8000:8000"
  #   env_file: .env
  #   depends_on: []

  # ai-service:
  #   build: ./ai-service
  #   ports:
  #     - "8001:8001"
  #   env_file: .env

networks:
  default:
    name: phishguard-net
